# Implementation Plan

- [x] 1. StandardEffortMasterForm の基本レイアウト修正
- [x] 1.1 ラベル変更とフォームフィールド横並び配置
  - 事業部選択フィールドのラベルを「ビジネスユニット」に変更する
  - プレースホルダーを「ビジネスユニットを選択」に変更する
  - ビジネスユニットと案件タイプの選択フィールドを横並び（2カラム）で配置する
  - パターン名フィールドは別行で配置し、フォーム全体の幅制約を調整する
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.1, 2.2_

- [x] 1.2 カード背景色の修正とカード上下配置への変更
  - 重み配分カードにテーマ変数準拠の背景色を明示的に指定する
  - 重み配分カードとグラフカードの配置を左右（2カラム）から上下（1カラム）に変更する
  - _Requirements: 3.1, 4.2, 4.3_

- [x] 2. (P) WeightDistributionChart の背景色修正と高さ調整
  - カードの背景色をハードコーディングからテーマ変数準拠に変更する
  - グラフの高さを全幅カード前提の適切なサイズに調整する
  - 全幅表示時にグラフの傾向が視覚的に把握可能であることを確認する
  - _Requirements: 3.2, 6.1, 6.2_

- [x] 3. StandardEffortMasterForm のテーブル構造を横方向に転置
  - 重み配分テーブルを縦方向（21行×2列）から横方向（1行×21列）に変換する
  - ヘッダー行に進捗率（0%〜100%）を列として展開する
  - ボディ行に各進捗率の重み入力フィールドを配置する
  - 全21列が横スクロールなしで画面幅内に収まるようセル幅を均等配分する
  - 数値入力の全角→半角変換ロジックを維持する
  - 縦スクロール用のラッパーを削除する（1行のため不要）
  - _Requirements: 5.1, 5.2, 5.3_

- [x] 4. (P) StandardEffortMasterDetail のレイアウト修正
- [x] 4.1 カード上下配置と背景色の統一
  - 重み配分カードとグラフカードの配置を上下（1カラム）に変更する
  - カードの背景色をテーマ変数準拠に統一する
  - _Requirements: 4.1, 7.1_

- [x] 4.2 読み取り専用テーブルの横方向転置
  - 読み取り専用テーブルを横方向（1行×21列）に変換する
  - ヘッダー行に進捗率、ボディ行に重み値を表示する
  - フォーム画面と同一のテーブル構造・スペーシングを適用する
  - 縦スクロール用のラッパーを削除する
  - _Requirements: 5.1, 5.2, 5.3, 7.2_

- [x] 5. 統合検証とビルド確認
  - TypeScriptビルドがエラーなしで完了することを確認する
  - リントチェックがエラーなしで完了することを確認する
  - 新規登録画面・詳細画面・編集画面の全画面でレイアウトが統一されていることを確認する
  - _Requirements: 7.3_
