# 実装計画

- [x] 1. (P) Project型とZodスキーマに7フィールドを追加する
  - `Project` インターフェースに `fiscalYear`, `nickname`, `customerName`, `orderNumber`, `calculationBasis`, `remarks`, `region` を null 許容で追加する
  - `createProjectSchema` に7フィールドのバリデーションルールを追加する（数値は整数、文字列は最大文字数制約付き、すべて任意）
  - `updateProjectSchema` に7フィールドを nullable かつ optional で追加する
  - バックエンドAPIレスポンス型と一致していることを確認する
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_

- [x] 2. (P) 複数行テキスト入力用の Textarea コンポーネントを作成する
  - shadcn/ui パターンに準拠した Textarea コンポーネントを新規作成する
  - 既存の Input コンポーネントと同じスタイリングパターン（クラス名マージ、ref転送）を踏襲する
  - _Requirements: 2.4_

- [x] 3. 案件フォームに7つの入力フィールドを追加する
  - フォーム値の型に7フィールドを追加する（文字列は空文字列デフォルト、年度は null 許容数値）
  - `useForm` のデフォルト値に7フィールドの初期値を追加する
  - 年度フィールドは既存の期間月数と同じ nullable 数値入力パターンで実装する
  - 通称・略称、客先名、オーダー番号、地域は既存のテキスト入力パターンで実装する
  - 算出根拠、備考はテキストエリアを使用して複数行入力に対応する
  - 各フィールドに Zod スキーマによる文字数バリデーションを適用する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

- [x] 4. (P) 案件一覧テーブルに年度・客先名・オーダー番号の3列を追加する
  - 種別列の直後に年度、客先名、オーダー番号の3列を挿入する
  - 各列にソート可能なヘッダーを適用する
  - 空値はemダッシュ（—）で表示する
  - 他の4フィールド（通称・略称、算出根拠、備考、地域）はテーブルに表示しない
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 5. (P) 案件詳細画面に7フィールドの表示を追加する
  - 詳細カードの props 型に7フィールドを追加する
  - 年度、通称・略称、客先名、オーダー番号、算出根拠、備考、地域をラベル付きで表示する
  - 空値はemダッシュ（—）で表示する
  - ラベルには日本語名を使用する
  - _Requirements: 4.1, 4.2, 4.3_

- [x] 6. 新規作成・編集画面のフォームデータ受け渡しを7フィールドに対応させる
- [x] 6.1 (P) 編集画面の handleSubmit 型と defaultValues を7フィールドに対応させる
  - `handleSubmit` の values 型に7フィールドを追加する
  - `defaultValues` で API レスポンスの null 値を空文字列に変換してフォームに渡す
  - `mutateAsync` 呼び出し時に空文字列を undefined に変換してAPIに送信する
  - _Requirements: 2.6, 2.7, 5.1_

- [x] 6.2 (P) 新規作成画面の handleSubmit 型を7フィールドに対応させる
  - `handleSubmit` の values 型に7フィールドを追加する
  - `mutateAsync` 呼び出し時に空文字列を undefined に変換してAPIに送信する
  - _Requirements: 2.6, 5.1_

- [x] 7. TypeScript ビルドと既存テストで互換性を検証する
  - フロントエンドの TypeScript ビルドがエラーなく完了することを確認する
  - 既存のテストスイートがすべてパスすることを確認する
  - _Requirements: 5.1, 5.2, 5.3_
