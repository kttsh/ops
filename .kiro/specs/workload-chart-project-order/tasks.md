# Implementation Plan

- [x] 1. (P) チャートデータフックに案件シリーズの並び順ソート機能を追加する
  - オプション設定に案件 ID 配列による並び順指定を受け取れるようにする
  - 案件シリーズのみを指定順序でソートし、間接作業シリーズの位置は維持する
  - 凡例パネル用の月別データでも案件一覧を同一の並び順でソートする
  - 並び順が指定されていない場合は API レスポンス順（既存動作）を維持する
  - 並び順に含まれない案件はソート結果の末尾に配置する
  - メモ化の依存配列に並び順を追加し、変更時に再計算されるようにする
  - _Requirements: 1.2, 1.3, 2.1, 2.2, 3.2, 4.2_
  - _Contracts: useChartData State_

- [x] 2. (P) 設定パネルから並び順の変更を親コンポーネントに通知する
  - 設定パネルの props に並び順変更の通知コールバックを追加する
  - 並び順 state を監視する単一の副作用で通知コールバックを呼び出す（全変更契機を一箇所でカバー）
  - コールバック参照の安定化パターン（ref）を適用し、副作用の依存配列を安定させる
  - 並び替え操作・プロファイル適用・初期ロードのいずれでも通知が発火することを確認する
  - _Requirements: 1.1, 3.1_
  - _Contracts: SidePanelSettings State_

- [x] 3. ページコンポーネントで並び順の state 管理と全体統合を行う
  - 並び順 state とその更新ハンドラを追加する
  - 設定パネルに並び順通知コールバックを渡す
  - チャートデータフックのオプションに並び順を渡す
  - 色設定の橋渡しパターンと同一構造であることを確認する
  - TypeScript strict mode で型エラーがないことを確認する
  - _Requirements: 1.1, 3.1, 4.1_
  - _Contracts: WorkloadPage State_
