# Implementation Plan

- [x] 1. (P) 間接業務比率の百分率変換に丸め処理を追加する
  - DB から取得した比率値（0〜1）を百分率（0〜100）に変換する際、整数化を経由して浮動小数点の丸め誤差を排除する
  - `ratio * 100` の代わりに `Math.round(ratio * 10000) / 100` を適用し、小数第2位までの精度を保証する
  - 7, 33.3, 50 等の代表的な入力値で変換結果が正確であることを確認する
  - 保存ロジック（百分率→小数への逆変換）には変更を加えない
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. (P) Recharts チャートのフォーカス枠を CSS で抑制する
  - グローバルスタイルに Recharts SVG 要素のフォーカス outline を無効化するルールを追加する
  - セレクタを `.recharts-surface` スコープに限定し、他のUI要素への影響を防止する
  - Workload ダッシュボードおよびケーススタディのチャートの両方がカバーされることを確認する
  - _Requirements: 2.1, 2.2, 2.3, 2.4_

- [x] 3. ビルド・テスト・Lint による回帰確認を実行する
  - TypeScript コンパイル、テストスイート、ESLint がすべてエラーなしで通過することを確認する
  - タスク1・2の完了後に実行する
  - _Requirements: 3.1, 3.2, 3.3_
