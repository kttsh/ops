# Implementation Plan

- [x] 1. useIndirectSimulation フックの作成
  - `indirect-case-study` feature 内にシミュレーション専用のオーケストレーションフックを新規作成する
  - ケース・シナリオの選択状態（人員計画ケース、キャパシティシナリオ、間接作業ケース）を管理する
  - BU変更時にすべての選択状態と計算結果をリセットする
  - 人員計画ケース一覧、キャパシティシナリオ一覧、間接作業ケース一覧をクエリで取得する
  - 間接作業ケース選択時に比率データと月次人員データをクエリで取得する
  - 既存の `useCapacityCalculation` を利用してキャパシティ計算を実行する
  - 既存の `useIndirectWorkCalculation` を利用して間接作業計算を実行する
  - 計算可否条件を算出する（キャパシティ: ケース＋シナリオ選択済み、間接作業: キャパシティ計算完了＋ケース選択済み）
  - 既存の `useBulkSaveMonthlyIndirectWorkLoads` を利用して計算結果の保存機能を提供する
  - dirty 管理を実装する（計算完了時に `true`、保存成功時に `false`）
  - 結果表示用の年度状態を管理する
  - `indirect-case-study` の `index.ts` に `useIndirectSimulation` のエクスポートを追加する
  - _Requirements: 2.2, 3.1, 3.2, 4.1, 4.2, 4.3, 4.4, 5.1, 5.2, 5.3, 5.4, 5.5, 7.1, 7.2, 7.3, 7.4_

- [x] 2. (P) ルート定義と SidebarNav 更新
  - `/indirect/simulation` のルート定義ファイルを作成し、`bu` パラメータの Zod スキーマバリデーションを設定する
  - lazy loading 用のルートファイルを空のプレースホルダーとして作成する（ページ実装はタスク3で実施）
  - SidebarNav の「案件・間接作業管理」セクションに「間接作業シミュレーション」リンクを追加する（Play アイコン使用、「間接作業・キャパシティ」の直後に配置）
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 3. シミュレーション画面のページコンポーネント実装
  - lazy route ファイルにシミュレーション画面の全体レイアウト（左パネル＋右パネルの2カラム構成）を実装する
  - URL Search Params からBUコードを取得し、BU一覧をドロップダウンで表示する（未指定時は先頭BU自動選択）
  - BU選択時に URL を更新し、ケース・シナリオデータの再読み込みをトリガーする
  - 左パネルに人員計画ケース、キャパシティシナリオ、間接作業ケースの3つのドロップダウンを配置する（読み取り専用選択、CRUD機能なし）
  - 左パネルにキャパシティ計算ボタンと間接作業計算ボタンを配置し、計算可否条件に応じて有効/無効を制御する
  - 計算中はローディング状態を表示し、エラー時はトースト通知を表示する
  - 右パネルに既存の ResultPanel を配置し、計算結果の表示・保存・エクスポート・インポート機能を統合する
  - 作業種類データを `workTypesQueryOptions` から取得し、ResultPanel に渡す
  - 計算結果がない場合は空状態メッセージを表示する
  - BU未選択時は計算関連の操作を無効化する
  - _Requirements: 1.1, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 6.1, 6.2, 6.3, 7.1, 7.2, 7.3, 7.4, 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 9.4_

- [x] 4. 統合検証
  - 開発サーバーで SidebarNav からシミュレーション画面に遷移できることを確認する
  - BU選択 → ケース/シナリオ選択 → キャパシティ計算 → 間接作業計算 → 結果表示の一連のフローが動作することを確認する
  - 計算結果の保存、Excelエクスポート、Excelインポートが正常に動作することを確認する
  - TypeScript の型チェックとリントが通ることを確認する
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3, 4.4, 5.1, 5.2, 5.3, 5.4, 5.5, 6.1, 6.2, 6.3, 7.1, 7.2, 7.3, 7.4, 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 9.4_

- [x]* 5. useIndirectSimulation のユニットテスト
  - BU変更時に選択状態と計算結果がリセットされることをテストする
  - 計算可否条件（canCalculateCapacity、canCalculateIndirectWork）が正しく算出されることをテストする
  - キャパシティ計算と間接作業計算の呼び出し連携をテストする
  - 保存成功時に dirty フラグが false にリセットされることをテストする
  - _Requirements: 2.2, 4.1, 4.2, 5.1, 5.2, 5.3, 7.1_
