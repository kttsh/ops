# ユーザーフィードバックに基づく改修要件定義書

> **Version**: 0.3.0
> **Last Updated**: 2026-03-09
> **Status**: 開発プラン策定済み

---

## 目次

1. [概要](#1-概要)
2. [開発プラン](#2-開発プラン)
3. [Issue一覧サマリ](#3-issue一覧サマリ)
4. [Issue詳細](#4-issue詳細)
5. [Open Questions](#5-open-questions)
6. [更新履歴](#6-更新履歴)

---

## 1. 概要

本文書は、ユーザー試行（UAT）で得られたフィードバック13件+追加2件を整理し、改修要件として定義するものである。

### 1.1 分類

| カテゴリ | 件数 | Issue番号 |
|----------|:----:|-----------|
| 機能追加 | 5 | #1, #5, #9, #10, #11 |
| UX改善 | 5 | #2, #3, #8, #13, #14 |
| バグ修正 | 3 | #6, #7, #12 |
| UIリファクタ | 1 | #4 |

### 1.2 優先度（案）

| 優先度 | Issue |
|--------|-------|
| 高（バグ） | #6, #7, #12 |
| 高（業務影響大） | #1, #5, #10, #11 |
| 中 | #3, #4, #8, #9, #13, #14 |
| 低 | #2 |

---

## 2. 開発プラン

### 2.1 方針

- 各要件Issueに対応するGitHub Issueを起票し、進捗を管理する
- 実装はcc-sdd（Spec-Driven Development）の Spec 単位で進める
- 小さな修正はまとめて1 Specに、複雑な機能は個別 Spec とする
- 各フェーズ内の順序は依存関係を考慮して決定

### 2.2 フェーズ別開発計画

#### Phase 1: クイックフィックス（低リスク・即効性あり）

| 順序 | 要件 | GitHub Issue | Spec | 概要 | 進捗 |
|:----:|:----:|:------------:|------|------|:----:|
| 1-1 | #12 | [#55](https://github.com/kttsh/ops/issues/55) | `frontend-bugfixes-55-56` | 浮動小数点精度エラー修正 | 🟡 実装済み |
| 1-2 | #6 | [#56](https://github.com/kttsh/ops/issues/56) | `frontend-bugfixes-55-56` | チャート黒枠修正 | 🟡 実装済み |
| 1-3 | #3 | [#57](https://github.com/kttsh/ops/issues/57) | `quick-fixes` | テーブル幅最大化 | ⬜ 未着手 |
| 1-4 | #13 | [#58](https://github.com/kttsh/ops/issues/58) | `quick-fixes` | シミュレーション説明テキスト修正 | ⬜ 未着手 |

> **Spec `quick-fixes`**: 4件の小規模修正を1 Spec にまとめて実施。各修正は独立しているため、1タスクずつ順に実装。

#### Phase 2: 単体機能追加（中リスク・独立性あり）

| 順序 | 要件 | GitHub Issue | Spec | 概要 | 進捗 |
|:----:|:----:|:------------:|------|------|:----:|
| 2-1 | #7 | [#59](https://github.com/kttsh/ops/issues/59) | `bugfix-export` | エクスポート不具合の調査・修正 | ⬜ 未着手 |
| 2-2 | #11 | [#60](https://github.com/kttsh/ops/issues/60) | `feat-order-number` | オーダー番号属性追加（DB〜UI全層） | ⬜ 未着手 |
| 2-3 | #8,#14 | [#61](https://github.com/kttsh/ops/issues/61) | `ux-standard-effort-layout` | 標準工数パターン画面レイアウト改善 | 🟡 実装済み |
| 2-4 | #4 | [#62](https://github.com/kttsh/ops/issues/62) | `feat-work-type-ordering` | 作業種類の表示順入れ替え | 🟡 実装済み |
| 2-5 | #1 | [#63](https://github.com/kttsh/ops/issues/63) | `feat-bulk-interpolation` | 按分入力（線形補間）機能 | 🟡 実装済み |

> 各 Issue が独立した Spec。#7 は調査フェーズが先行するため Phase 2 の先頭に配置。

#### Phase 3: 複合機能（高リスク・相互依存あり）

| 順序 | 要件 | GitHub Issue | Spec | 概要 | 進捗 |
|:----:|:----:|:------------:|------|------|:----:|
| 3-1 | #5 | [#64](https://github.com/kttsh/ops/issues/64) | `feat-case-selection` | 案件ケース選択機能（API+UI） | ⬜ 未着手 |
| 3-2 | #9 | [#65](https://github.com/kttsh/ops/issues/65) | `feat-indirect-loads-screen` | 間接工数の閲覧・編集画面（新規画面） | ⬜ 未着手 |
| 3-3 | #10 | [#66](https://github.com/kttsh/ops/issues/66) | `feat-capacity-auto-generation` | キャパシティ自動生成（n×m組み合わせ） | ⬜ 未着手 |

> #10 は #5 のチャート設定基盤を活用するため、#5 の後に実施。#9 は独立だが新規画面のため Phase 3。

#### Phase 4: 低優先度

| 順序 | 要件 | GitHub Issue | Spec | 概要 | 進捗 |
|:----:|:----:|:------------:|------|------|:----:|
| 4-1 | #2 | [#67](https://github.com/kttsh/ops/issues/67) | `ux-year-selector` | 年度セレクタ改善（複数年度同時表示） | ⬜ 未着手 |

### 2.3 Spec 一覧

| Spec名 | 対応要件 | フェーズ | 規模 | 状態 |
|---------|----------|:--------:|:----:|:----:|
| `frontend-bugfixes-55-56` | #12, #6 | Phase 1 | S | 🟡 実装済み |
| `quick-fixes` | #3, #13 | Phase 1 | S | ⬜ 未着手 |
| `bugfix-export` | #7 | Phase 2 | M | ⬜ 未着手 |
| `feat-order-number` | #11 | Phase 2 | M | ⬜ 未着手 |
| `ux-standard-effort-layout` | #8, #14 | Phase 2 | M | 🟡 実装済み |
| `feat-work-type-ordering` | #4 | Phase 2 | M | 🟡 実装済み |
| `feat-bulk-interpolation` | #1 | Phase 2 | M | 🟡 実装済み |
| `feat-case-selection` | #5 | Phase 3 | L | ⬜ 未着手 |
| `feat-indirect-loads-screen` | #9 | Phase 3 | L | ⬜ 未着手 |
| `feat-capacity-auto-generation` | #10 | Phase 3 | L | ⬜ 未着手 |
| `ux-year-selector` | #2 | Phase 4 | M | ⬜ 未着手 |

### 2.4 進捗記号の凡例

| 記号 | 意味 |
|:----:|------|
| ⬜ | 未着手 |
| 🔵 | Spec作成中（Requirements/Design/Tasks） |
| 🟡 | 実装中 |
| 🟢 | 完了（PR マージ済み） |
| ⏸️ | ブロック中（Open Questions 等） |

---

## 3. Issue一覧サマリ

| # | 画面 | カテゴリ | 概要 | 複雑度 |
|---|------|----------|------|:------:|
| 1 | `/master/headcount-plans` | 機能追加 | 一括入力で初期値→到達値の按分入力 | 中 |
| 2 | `/master/headcount-plans` | UX改善 | 年度プルダウンの操作性改善（複数年度同時表示） | 低 |
| 3 | `/projects` | UX改善 | テーブル幅の左右最大化 | 低 |
| 4 | `/master/work-types` | UIリファクタ | 間接作業の層別の順番入れ替え | 中 |
| 5 | `/workload` | 機能追加 | 案件ケース選択機能 | 高 |
| 6 | `/workload` | バグ修正 | Areaチャートクリック時の黒枠 | 低 |
| 7 | `/projects` | バグ修正 | エクスポート機能の不具合 | 中 |
| 8 | `/projects/standard-efforts` | UX改善 | 閲覧画面のレイアウト変更 | 中 |
| 9 | `/indirect/simulation` | 機能追加 | 計算結果の閲覧・編集画面 | 高 |
| 10 | `/workload` | 機能追加 | キャパシティ自動生成（人員計画×稼働時間） | 高 |
| 11 | 全体 | 機能追加 | 案件にオーダー番号属性を追加 | 中 |
| 12 | `/master/indirect-work-cases` | バグ修正 | 浮動小数点精度エラー | 低 |
| 13 | `/indirect/simulation` | UX改善 | 左パネルUI説明の不整合修正 | 低 |
| 14 | `/projects/standard-efforts/new` | UX改善 | 新規登録画面のレイアウト改善 | 中 |

---

## 4. Issue詳細

### Issue #1: 一括入力で初期値→到達値の按分入力

**画面**: `/master/headcount-plans`
**カテゴリ**: 機能追加
**報告内容**: 一括入力で初期値と到達値から按分。4月と3月を指定して、中間の月は四捨五入でばらす。

#### 現状の実装

- **一括入力ダイアログ** (`BulkInputDialog.tsx`): 年度と人員数を指定し、全12ヶ月に**同一値**を設定する
- 按分（線形補間）機能は未実装
- 月グリッド: `MonthlyHeadcountGrid.tsx` で4月〜翌3月の12ヶ月入力

#### 要件

| 項目 | 内容 |
|------|------|
| 機能名 | 按分入力（線形補間）|
| 入力パラメータ | 開始月値（4月）、終了月値（3月）|
| 計算ロジック | 線形補間で中間月を算出、四捨五入で整数化 |
| 対象期間 | 選択した年度の12ヶ月固定（4月〜翌3月） |
| UI | 既存の一括入力ダイアログに「按分モード」を追加（トグル切替）|

#### 計算ロジック例

```
開始値: 10人（4月）、到達値: 15人（3月）
ステップ = (15 - 10) / 11 = 0.4545...

4月: 10, 5月: round(10.45)=10, 6月: round(10.91)=11, 7月: round(11.36)=11,
8月: round(11.82)=12, 9月: round(12.27)=12, 10月: round(12.73)=13,
11月: round(13.18)=13, 12月: round(13.64)=14, 1月: round(14.09)=14,
2月: round(14.55)=15, 3月: 15
```

#### 影響範囲

| ファイル | 変更内容 |
|----------|---------|
| `BulkInputDialog.tsx` | 按分モードUI追加、計算ロジック追加 |
| `useHeadcountPlansPage.ts` | 必要に応じてインターフェース変更 |

#### Open Questions

- [Q1-2] 既存の「全月同一値」モードと按分モードはどちらをデフォルトにすべきか？
- [Q1-3] 年度をまたいだ按分（例: 2025年度4月→2026年度3月の24ヶ月）は必要か？

---

### Issue #2: 年度ごとのプルダウンの操作性改善

**画面**: `/master/headcount-plans`
**カテゴリ**: UX改善
**報告内容**: 年度ごとのプルダウンはまどろっこしい

#### ユーザーの具体的な課題

年度を都度切り替えた上で12ヶ月の数字を入力しなければならない。一方で画面スペース・空白は大量に空いている。有効活用して、例えば5年分を同時に表示したい。

#### 現状の実装

- `MonthlyHeadcountGrid.tsx` 内に `<Select>` で年度選択
- 11年分の選択肢（現在年度 ±2 〜 +8）
- 年度切替で12ヶ月分のグリッドを更新

#### 改善案

| 案 | 内容 | メリット | デメリット |
|----|------|----------|-----------|
| A（推奨） | 複数年度（5年分等）を縦方向に同時表示 | 年度切替不要、画面スペースを有効活用 | 表示行数が増加 |
| B | 前年/翌年ボタン + プルダウン併用 | 近い年度は素早く、遠い年度もアクセス可能 | 根本的な課題は解消しない |

#### 影響範囲

- `MonthlyHeadcountGrid.tsx`: 年度セレクタUIの変更、複数年度グリッドへの拡張
- 同パターンを使用する画面: `/indirect/simulation` の `CalculationResultTable.tsx` でも年度セレクタあり

#### Open Questions

- [Q2-2] 同時表示する年度数は何年分が適切か？（5年分？全期間？スクロール上限？）

---

### Issue #3: `/projects` テーブル幅の左右最大化

**画面**: `/projects`（案件一覧）
**カテゴリ**: UX改善
**報告内容**: テーブル幅を左右に最大化したい

#### 現状の実装

- `AppShell.tsx` (L79-88): `/projects` パスは `mx-auto max-w-4xl px-6 py-8` が適用される
- `max-w-4xl` = 896px がコンテンツの最大幅
- `/workload` と `/master/*` のみ `h-full`（幅制約なし）

#### 要件

**決定事項**: `/projects` 配下の全画面で幅制約を解除する（案A採用）。

`/projects*` パスを `/workload` や `/master/*` と同様に `h-full` にする。フォーム系画面（`/projects/new`, `/projects/$projectId/edit`）は `max-w-md` をフォーム自身が持っているため、親の幅制約を解除しても問題ない。

#### 影響を受ける画面

| パス | 画面名 | テーブルあり |
|------|--------|:----:|
| `/projects` | 案件一覧 | ○ |
| `/projects/new` | 案件新規登録 | - |
| `/projects/$projectId` | 案件詳細 | - |
| `/projects/$projectId/edit` | 案件編集 | - |
| `/projects/standard-efforts` | 標準工数パターン一覧 | ○ |
| `/projects/standard-efforts/new` | 標準工数パターン新規登録 | - |
| `/projects/standard-efforts/$id` | 標準工数パターン詳細 | - |

#### 影響範囲

| ファイル | 変更内容 |
|----------|---------|
| `AppShell.tsx` | パス判定条件に `/projects*` を `h-full` 対象として追加 |

#### Open Questions

- [Q3-2] 他の画面（例: `/`トップページ）も同様に幅を広げたいか？

---

### Issue #4: 間接作業の層別（作業種類）の積み上げ順入れ替え

**画面**: `/workload`（ワークロードダッシュボード）> コントロールパネル > 間接作業タブ
**カテゴリ**: UIリファクタ
**報告内容**: 間接作業の層別を上下させたとき、間接作業内で順番を入れ替えてほしい

#### 現状の実装

- `SidePanelIndirect.tsx` に↑↓ボタン（`ArrowUp` / `ArrowDown`）が**実装済み**
- `chart_stack_order_settings` テーブルに積み上げ順を保存（`PUT /chart-stack-order-settings/bulk`）
- マスタ管理画面（`/master/work-types`）での順番入れ替えは**行わない**

#### 要件

| 項目 | 内容 |
|------|------|
| 機能 | ワークロード画面のコントロールパネル > 間接作業タブで積み上げ順を上下ボタンで変更 |
| 反映先 | ワークロードチャートの間接作業積み上げ順序のみ |
| 重要な制約 | **間接作業の Area は必ず案件の Area の下部に配置**。順番入れ替えは間接作業 Area 内での相対的な順序変更のみ |

#### 影響範囲

| ファイル | 変更内容 |
|----------|---------|
| `SidePanelIndirect.tsx` | 既存の↑↓ボタンの動作検証・不足分の補完 |
| `WorkloadChart.tsx` | 間接作業が案件の下部に固定されている制約の検証 |

#### Open Questions

- なし（既存実装の検証・補完が中心）

---

### Issue #5: ダッシュボードで案件ケース選択機能

**画面**: `/workload`
**カテゴリ**: 機能追加
**報告内容**: 案件のケースのどれを表示するか選べるようにしたい

#### 現状の実装

- ダッシュボードの `GET /chart-data` API は `isPrimary = true` のケースを自動選択
- フロントエンドで案件の個別選択は可能（`SidePanelProjects.tsx`）
- **ケース（ProjectCase）の選択UIは存在しない**
- 各案件には複数ケースがあり得る（`project_cases` テーブル）

#### 要件

| 項目 | 内容 |
|------|------|
| 機能 | 各案件について、どのケースをチャートに反映するか**案件ごとに個別に**選択可能にする |
| デフォルト | `isPrimary = true` のケース |
| UI配置 | サイドパネル > 案件タブ内の各案件カードにケースセレクタを追加 |

#### 影響範囲

| レイヤー | 変更内容 |
|----------|---------|
| フロントエンド | `SidePanelProjects.tsx` にケース選択UIを追加 |
| フロントエンド | `useChartData.ts` にケースID指定パラメータを追加 |
| バックエンド | `GET /chart-data` API にケースID指定パラメータを追加（現状の `isPrimary` 自動選択をオーバーライド）|
| 型定義 | `ChartDataParams` にケース指定を追加 |

#### Open Questions

- [Q5-2] ケースが1つしかない案件では選択UIを非表示にすべきか？
- [Q5-3] ケース選択状態はプロファイル（ChartView）に保存すべきか？

---

### Issue #6: グラフエリアのArea部クリック時の黒枠

**画面**: `/workload`
**カテゴリ**: バグ修正
**報告内容**: グラフエリアのArea部をクリックすると黒枠が出る
**発生ブラウザ**: Chrome, Edge

#### 現状の実装

- `WorkloadChart.tsx` (L91): `style={{ outline: "none" }}` が `ComposedChart` に設定済み
- `activeDot={false}` で各 Area/Line のアクティブドットを無効化済み
- **しかしユーザーから黒枠が報告されている**

#### 原因推定

- `outline: "none"` は `ComposedChart` の SVG コンテナに対する設定
- **Area 要素自体** や **SVG `<path>` 要素** がフォーカスを受けた際のブラウザデフォルトスタイル（`outline` or CSS `focus` ring）が原因の可能性
- Recharts が内部で生成する `<path>` や `<rect>` に `tabIndex` が設定されている可能性

#### 修正案

```css
/* globals.css に追加 */
.recharts-surface:focus,
.recharts-surface *:focus {
  outline: none;
}
```

または `WorkloadChart.tsx` で:

```tsx
<ComposedChart
  style={{ outline: "none" }}
  accessibilityLayer={false}  // ← 既にfalse
  // 追加: cursor="pointer" をやめ、SVGのfocusableを無効化
>
```

#### 影響範囲

| ファイル | 変更内容 |
|----------|---------|
| `globals.css` | Recharts SVG要素のフォーカス枠を抑制 |
| `WorkloadChart.tsx` | 必要に応じて追加のスタイル設定 |

#### Open Questions

- [Q6-2] クリック時のみ発生か、Tabキーでフォーカス移動したときも発生するか？

---

### Issue #7: 案件のエクスポート機能の不具合

**画面**: `/projects`
**カテゴリ**: バグ修正
**報告内容**: 案件のエクスポート機能が機能しない

#### 現状の実装

- `useProjectBulkExport.ts`: `fetchExportData()` → `buildBulkExportWorkbook()` → `downloadWorkbook()`
- API: `GET /bulk/export-project-loads` からデータ取得
- `excel-utils.ts`: xlsx ライブラリでワークブック生成、Blob からダウンロード
- ファイル名: `案件工数一括_YYYYMMDD.xlsx`

#### 調査が必要な項目

| 観点 | 確認内容 |
|------|---------|
| APIレスポンス | `/bulk/export-project-loads` が正常にデータを返すか |
| フロントエンド | `fetchExportData()` の呼び出しがエラーにならないか |
| ダウンロード処理 | Blob生成とリンク生成が正常に動作するか |
| データ量 | 大量データ時にタイムアウトしていないか |

#### 影響範囲

| ファイル | 確認/修正 |
|----------|---------|
| `useProjectBulkExport.ts` | エクスポートフックの動作確認 |
| `bulk-client.ts` (projects) | API呼び出しの確認 |
| `excel-utils.ts` | ワークブック生成処理の確認 |
| バックエンド: `bulkExport` ルート | APIエンドポイントの確認 |

#### Open Questions

- [Q7-1] エクスポートボタンクリック後の挙動: 何も起きない？エラーメッセージが出る？ファイルが壊れている？
- [Q7-2] どのデータをエクスポートしようとしたときに失敗するか？（特定のBU、特定の案件数以上、等）
- [Q7-3] インポート機能は正常に動作するか？

---

### Issue #8: 標準工数パターン閲覧画面のレイアウト変更

**画面**: `/projects/standard-efforts/$standardEffortId`（詳細画面）
**カテゴリ**: UX改善
**報告内容**:
- 重み配分カードとグラフカードを上下に配置（現状は左右）
- 重み配分のテーブルは列に進捗率を5%刻みに

#### 現状の実装

- `StandardEffortMasterDetail.tsx`: `grid grid-cols-1 lg:grid-cols-2 gap-6` で左右配置
- 重み配分テーブル: 行に進捗率（0%〜100%の21行）、列は「進捗率」「重み」の2列
- グラフ: `WeightDistributionChart.tsx`（Recharts AreaChart、高さ256px）

#### 要件

**A. レイアウト変更**

| 項目 | 現状 | 変更後 |
|------|------|--------|
| カード配置 | `grid-cols-2`（左右） | `grid-cols-1`（上下） |
| 表示順 | 左: テーブル、右: グラフ | 上: テーブル、下: グラフ |

**B. テーブル構造の変更**

| 項目 | 現状 | 変更後 |
|------|------|--------|
| 行 | 21行（0%, 5%, ..., 100%） | 1行（重み値の行） |
| 列 | 2列（進捗率、重み） | 21列（0%, 5%, 10%, ..., 100%） |
| 方向 | 縦（進捗率が行） | 横（進捗率が列） |
| スクロール | - | **横スクロール不要**なスペーシングで実装（セル幅を詰める等の工夫） |

#### 影響範囲

| ファイル | 変更内容 |
|----------|---------|
| `StandardEffortMasterDetail.tsx` | グリッドを `grid-cols-1` に変更、テーブル構造を転置 |
| `StandardEffortMasterForm.tsx` | **同様の変更が必要**（Issue #14 と同一） |
| `WeightDistributionChart.tsx` | 幅が広くなるため、高さの調整が必要な可能性 |

#### 同型の画面（一緒に変更すべき）

| 画面 | ファイル | 同じ構造か |
|------|----------|:----:|
| 標準工数 詳細 | `StandardEffortMasterDetail.tsx` | ○ |
| 標準工数 新規登録 | `StandardEffortMasterForm.tsx` | ○ |
| 標準工数 編集 | `StandardEffortMasterForm.tsx`（共通）| ○ |

#### Open Questions

- [Q8-2] グラフの高さは256pxのままでよいか？上下配置だと画面下部に配置されるため、視認性を考慮して高さを変更すべきか？

---

### Issue #9: 間接工数の計算結果をダッシュボード以外でも閲覧・編集

**画面**: `/indirect/simulation`（間接工数計算）
**カテゴリ**: 機能追加
**報告内容**: 間接工数の計算結果をダッシュボード以外でもみたい。できれば変更したい。

#### 現状の実装

- `/indirect/simulation` に `ResultPanel` + `CalculationResultTable` で計算結果を表示
- 計算結果は「間接作業工数を保存」ボタンで `monthly_indirect_work_loads` テーブルに保存
- 保存後のデータは `/workload` ダッシュボードで参照可能
- **保存済み結果の閲覧・編集は `/indirect/simulation` で計算を再実行しないと見られない**

#### 要件

| 項目 | 内容 |
|------|------|
| 目的 | 保存済みの間接工数を閲覧・手動編集できる画面を提供 |
| 画面 | **新規画面**として作成（例: `/indirect/monthly-loads`） |
| 編集時 | `source` を `'manual'` に設定 |
| 構成 | 他のマスタ管理画面の構成に合わせるかは別途検討 |

#### 影響範囲

| レイヤー | 変更内容 |
|----------|---------|
| フロントエンド | 新規画面の作成（ルーティング、コンポーネント） |
| バックエンド | 既存API（`GET/PUT /indirect-work-cases/:id/monthly-indirect-work-loads`）で対応可能 |

#### Open Questions

- [Q9-2] 編集は個別月の値を直接変更する形式か？一括入力か？
- [Q9-3] `source: 'calculated'` のデータを手動で上書きしてよいか？上書き時に警告を出すか？
- [Q9-4] 計算結果との差分を可視化する機能は必要か？

---

### Issue #10: キャパシティラインの自動生成

**画面**: `/workload`
**カテゴリ**: 機能追加
**報告内容**: キャパシティの線は、人員計画と稼働時間の掛け算で自動作成する。人員計画n個、稼働時間mケースの場合、n×mケースつくられる。名称は「人員計画名（稼働時間ケース名）」とする。

#### 現状の実装

- キャパシティは `monthly_capacity` テーブルに計算済みの値として格納
- `capacity_scenarios` に `hours_per_person` パラメータあり（デフォルト160h）
- `POST /capacity-scenarios/:id/actions/calculate` で自動計算API実装済み
  - ただし1つのシナリオ × 1つの人員計画ケースの組み合わせで計算
- ダッシュボードでは `capacityScenarioIds` を指定してキャパシティラインを表示

#### 要件

| 項目 | 内容 |
|------|------|
| 計算 | 人員計画ケース(n個) × キャパシティシナリオ(m個) = n×m本のキャパシティライン |
| 名称 | `{人員計画ケース名}({キャパシティシナリオ名})` |
| 例 | 人員計画「標準」「増員」× シナリオ「定時160h」「残業180h」= 4本のライン |
| 表示 | ダッシュボードのチャートに破線で表示 |
| デフォルト | **全ラインはデフォルトOFF**。ユーザーがチャート設定画面で個別にONにして表示する |
| 選択UI | チャート設定画面にn×mの組み合わせ一覧を表示し、表示/非表示を切り替え可能に |

#### 影響範囲

**方式A: フロントエンドで組み合わせ生成**
- 全人員計画ケース × 全シナリオを取得
- フロントエンドで `headcount × hoursPerPerson` を計算
- チャートに動的にLine系列を追加

**方式B: バックエンドで一括計算**
- 新規API: `POST /capacity-scenarios/actions/calculate-all` 等
- 全組み合わせの `monthly_capacity` を生成
- ダッシュボード表示は既存の仕組みを利用

**方式C: `GET /chart-data` の拡張**
- ダッシュボード用APIが全組み合わせを動的に計算して返す
- DBに保存せずオンザフライ計算

> **設計メモ**: DBへの保存は極力避けたい意向あり。ただしチャートのハンドリング等で課題が生じる場合はDB保存も検討する。

#### Open Questions

- [Q10-2] 計算結果をDBに保存するか、表示時にオンザフライ計算するか？（極力保持したくないが、技術的制約による判断が必要）
- [Q10-4] 間接作業ケースとの組み合わせも必要か？（ネットキャパシティ = キャパシティ - 間接工数）

---

### Issue #11: 案件情報にオーダー番号属性を追加

**画面**: 全体（案件関連画面）
**カテゴリ**: 機能追加
**報告内容**: 案件情報の属性にオーダー番号（文字6桁）を追加する

#### 現状の実装

- `projects` テーブル: `order_number` カラムは存在しない
- 案件の入力フォーム: `ProjectForm.tsx`
- 案件の表示: `columns.tsx`（テーブル列定義）

#### 要件

| 項目 | 内容 |
|------|------|
| フィールド名 | `orderNumber` |
| DB型 | `NVARCHAR(6)` or `CHAR(6)` |
| バリデーション | 6文字固定、**英数字のみ**（正規表現: `/^[A-Za-z0-9]{6}$/`） |
| 必須/任意 | 任意（NULL許容） |
| 一意制約 | **不要**（同一オーダー番号を複数案件に紐付け可能） |

#### 影響範囲

| レイヤー | ファイル | 変更内容 |
|----------|----------|---------|
| DB | マイグレーションSQL | `ALTER TABLE projects ADD order_number NVARCHAR(6) NULL` |
| バックエンド型 | `types/project.ts` | スキーマに `orderNumber` 追加 |
| バックエンドData | `projectData.ts` | SELECT/INSERT/UPDATE に `order_number` 追加 |
| バックエンドTransform | `projectTransform.ts` | snake_case → camelCase 変換追加 |
| フロントエンド型 | `features/projects/types/index.ts` | 型に `orderNumber` 追加 |
| フロントエンドフォーム | `ProjectForm.tsx` | 入力フィールド追加 |
| フロントエンドテーブル | `columns.tsx` | テーブル列追加 |
| 案件詳細 | `$projectId/index.tsx` | 表示項目追加 |
| エクスポート | `useProjectBulkExport.ts` | エクスポートデータに追加 |

#### Open Questions

- [Q11-3] 既存の案件に対してオーダー番号を後から付与する運用か？
- [Q11-4] オーダー番号で検索・フィルタリングする要件はあるか？

---

### Issue #12: 浮動小数点精度エラー

**画面**: `/master/indirect-work-cases`
**カテゴリ**: バグ修正
**報告内容**: 「その他」に7と入れて保存すると、7.000000000000001と表示される

#### 現状の実装

- `IndirectWorkRatioMatrix.tsx`: UI上は0〜100%で入力
- 保存時に `ratio: ratioPercent / 100` で0〜1に変換
- DB: `DECIMAL(5,4)` で正確に保存
- 表示時: `ratio * 100` で百分率に戻す

#### 原因

JavaScript の IEEE 754 浮動小数点演算:
```javascript
7 / 100 = 0.07  // DB保存は正確（DECIMAL）
0.07 * 100 = 7.000000000000001  // JS演算の丸め誤差
```

#### 修正案

`IndirectWorkRatioMatrix.tsx` の表示値計算に丸め処理を追加:

```typescript
// Before
const displayValue = ratio * 100;

// After
const displayValue = Math.round(ratio * 10000) / 100;
// または
const displayValue = parseFloat((ratio * 100).toPrecision(10));
```

#### 影響範囲

| ファイル | 変更内容 |
|----------|---------|
| `IndirectWorkRatioMatrix.tsx` | 表示値の丸め処理追加 |

同様の問題が発生し得る画面:

| 画面 | ファイル | 百分率変換 |
|------|----------|:----:|
| `/master/indirect-work-cases` | `IndirectWorkRatioMatrix.tsx` | ○ |
| `/indirect/simulation` | `CalculationResultTable.tsx` | △（比率表示がある場合）|

#### Open Questions

- なし（原因と修正方法が明確）

---

### Issue #13: `/indirect/simulation` 左パネルの不整合

**画面**: `/indirect/simulation`
**カテゴリ**: UX改善
**報告内容**: 「左パネルでケースとシナリオを選択し、計算を実行してください」⇒左パネルはない

#### 現状の実装

- `indirect/simulation/index.lazy.tsx`: 垂直レイアウト（上部にセレクタ、下部に結果）
- **左パネルは存在しない**（左右2カラム構成ではない）
- ただし既存の要件定義書 `indirect-requirements.md` では左右2カラム構成が記載されている

#### 要件

| 項目 | 内容 |
|------|------|
| 問題 | UIの説明テキストが実際のレイアウトと一致していない |
| 対応 | 画面内のガイドテキスト / ヘルプテキストを実際のUIに合わせて修正 |
| 代替 | または、要件定義書に基づいて左右2カラムレイアウトに改修 |

#### Open Questions

- [Q13-1] 現状の垂直レイアウトのまま説明テキストを修正するか？要件定義書どおり左右2カラムに改修するか？（改修時に判断予定）
- [Q13-2] 「左パネルでケースとシナリオを選択し〜」というテキストはどこに表示されているか？（画面内？ドキュメント？）

---

### Issue #14: 標準工数パターン新規登録画面のレイアウト改善

**画面**: `/projects/standard-efforts/new`
**カテゴリ**: UX改善
**報告内容**:
- 事業部をビジネスユニットに改称
- 案件タイプとビジネスユニットは横並び
- カードが透過している
- 重み配分カードとグラフカードを上下に配置（現状は左右）
- 重み配分のテーブルは列に進捗率を5%刻みに

#### 現状の実装

- `StandardEffortMasterForm.tsx`:
  - ラベル「事業部」（L77付近）→ 「ビジネスユニット」に変更必要
  - フォームフィールド: 縦並び（`grid grid-cols-1 gap-6 max-w-md`）
  - 重み配分カード + グラフカード: `grid grid-cols-1 lg:grid-cols-2 gap-6`（左右配置）
- カードスタイル: `rounded-3xl border p-6`（`bg-card` = `#ffffff`）
  - **透過はCSSレベルでは確認できず**。`bg-card` がCSS変数で定義されており、実際のレンダリング時に透過が見える可能性

#### 要件

**A. ラベル修正**

| 現状 | 変更後 |
|------|--------|
| 事業部 | ビジネスユニット |
| Placeholder「事業部を選択」 | 「ビジネスユニットを選択」 |

**B. フィールドレイアウト**

| 現状 | 変更後 |
|------|--------|
| ビジネスユニット + 案件タイプ: 縦並び | 横並び（`grid-cols-2`） |

**C. カード透過の修正**

| 現状 | 変更後 |
|------|--------|
| `rounded-3xl border p-6` | `rounded-3xl border p-6 bg-card`（明示的に背景色指定）|

**D. カード配置**（Issue #8 と共通）

| 現状 | 変更後 |
|------|--------|
| `grid-cols-2`（左右） | `grid-cols-1`（上下） |

**E. テーブル構造**（Issue #8 と共通）

| 現状 | 変更後 |
|------|--------|
| 行: 進捗率、列: 「進捗率」「重み」 | 行: 重み値、列: 進捗率（0%〜100%を5%刻み） |

#### 影響範囲

| ファイル | 変更内容 |
|----------|---------|
| `StandardEffortMasterForm.tsx` | A〜E全ての変更 |
| `StandardEffortMasterDetail.tsx` | D, E の変更（Issue #8 と同じ） |
| `WeightDistributionChart.tsx` | 配置変更に伴う高さ調整 |

#### Open Questions

- [Q14-1] 「カードが透過している」は特定のブラウザ/OSのみの現象か？（ダークモード対応の問題の可能性）

---

## 5. Open Questions まとめ

### 要件確定に必須

| ID | Issue | 質問 |
|----|-------|------|
| Q7-1 | #7 | エクスポートボタンクリック後の挙動: 何も起きない？エラーメッセージが出る？ファイルが壊れている？ |
| Q10-2 | #10 | 計算結果をDBに保存するか、表示時にオンザフライ計算するか？（極力保持したくないが、技術的制約による判断が必要） |

### 設計判断が必要

| ID | Issue | 質問 |
|----|-------|------|
| Q1-2 | #1 | 既存の「全月同一値」モードと按分モードはどちらをデフォルトにすべきか？ |
| Q1-3 | #1 | 年度をまたいだ按分（例: 2025年度4月→2026年度3月の24ヶ月）は必要か？ |
| Q2-2 | #2 | 同時表示する年度数は何年分が適切か？（5年分？全期間？スクロール上限？） |
| ~~Q4-2~~ | ~~#4~~ | ~~順番変更は即座にサーバーに保存か、一括保存か？~~ → **解決済み**: 既存実装で即座に保存 |
| Q5-2 | #5 | ケースが1つしかない案件では選択UIを非表示にすべきか？ |
| Q5-3 | #5 | ケース選択状態はプロファイル（ChartView）に保存すべきか？ |
| Q9-2 | #9 | 編集は個別月の値を直接変更する形式か？一括入力か？ |
| Q9-3 | #9 | `source: 'calculated'` のデータを手動で上書きしてよいか？上書き時に警告を出すか？ |
| Q9-4 | #9 | 計算結果との差分を可視化する機能は必要か？ |
| Q10-4 | #10 | 間接作業ケースとの組み合わせも必要か？（ネットキャパシティ = キャパシティ - 間接工数） |
| Q13-1 | #13 | 現状の垂直レイアウトのまま説明テキストを修正するか？要件定義書どおり左右2カラムに改修するか？（改修時に判断予定） |

### 確認事項

| ID | Issue | 質問 |
|----|-------|------|
| Q3-2 | #3 | 他の画面（例: `/`トップページ）も同様に幅を広げたいか？ |
| Q6-2 | #6 | 黒枠はクリック時のみ発生か、Tabキーでフォーカス移動したときも発生するか？ |
| Q7-2 | #7 | どのデータをエクスポートしようとしたときに失敗するか？（特定のBU、特定の案件数以上、等） |
| Q7-3 | #7 | インポート機能は正常に動作するか？ |
| Q8-2 | #8 | グラフの高さは256pxのままでよいか？上下配置で視認性を考慮して変更すべきか？ |
| Q11-3 | #11 | 既存の案件に対してオーダー番号を後から付与する運用か？ |
| Q11-4 | #11 | オーダー番号で検索・フィルタリングする要件はあるか？ |
| Q13-2 | #13 | 「左パネルでケースとシナリオを選択し〜」というテキストはどこに表示されているか？ |
| Q14-1 | #14 | 「カードが透過している」は特定のブラウザ/OSのみの現象か？ |

---

## 6. 更新履歴

| 日付 | バージョン | 内容 |
|------|-----------|------|
| 2026-03-09 | 0.3.0 | 開発プラン策定、GitHub Issue 13件起票（#55〜#67）、Spec単位・フェーズ定義 |
| 2026-03-09 | 0.2.0 | ユーザー回答を要件に反映、解消済みQAを統合・削除、Open Questions再整理 |
| 2026-03-09 | 0.1.0 | 初版作成（UAT フィードバック13件+2件の要件整理） |
